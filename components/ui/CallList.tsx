import React from 'react'

const CallList = ({ type }: { type: 'ended' | 'upcoming' | 'recordings' }) => {
    return (
        <div>
            Callist
        </div>
    )
}

export default CallList
